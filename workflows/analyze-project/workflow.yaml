name: analyze-project
description: 'Auto-detect the host project stack. Use when user says "analyze project" or "detect stack" or "what tech is this"'

config_source: "{project-root}/AI-QA-FRAMEWORK/config.yaml"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"

installed_path: "{project-root}/AI-QA-FRAMEWORK/workflows/analyze-project"
instructions: "{installed_path}/instructions.xml"
