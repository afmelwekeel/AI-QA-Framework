name: generate-test-data
description: 'Generate JSON test data files (valid, edge, security) for a suite (Phase 4). Use when user says "generate test data" or runs /aiqa-generatetestdata'

config_source: "{project-root}/AI-QA-FRAMEWORK/config.yaml"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
document_output_language: "{config_source}:document_output_language"
reporting_language: "{config_source}:reporting_language"
output_folder: "{config_source}:output_folder"
project_name: "{config_source}:project_name"
test_users: "{config_source}:test_users"
db_connection_string: "{config_source}:db_connection_string"

installed_path: "{project-root}/AI-QA-FRAMEWORK/workflows/generate-test-data"
instructions: "{installed_path}/instructions.xml"

# Runtime variables — resolved from TestResult/test-suites.yml
suite_name: ""     # From argument or active_suite in TestResult/test-suites.yml
story_id: ""       # Same value as suite_name
