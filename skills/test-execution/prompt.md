# Skill: Test Execution

Execute the run-tests workflow:

```
Load and follow: {project-root}/AI-QA-FRAMEWORK/workflows/run-tests/instructions.xml
```

Resolve all `{variables}` from `{project-root}/AI-QA-FRAMEWORK/config.yaml` and `{project-root}/AI-QA-FRAMEWORK/workflows/run-tests/workflow.yaml` before executing.
